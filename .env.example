# Gemini wird ausschließlich vom lokalen Node.js-Proxy verwendet.
# Diese Datei kopieren, als .env speichern und niemals committen.
GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Der Server bindet fest an 127.0.0.1. Hier nur den lokalen Port ändern.
API_PORT=8787
